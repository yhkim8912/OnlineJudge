#include <iostream>
#include <cstdio>
#define _USE_MATH_DEFINES
#include <math.h>

using namespace std;

int main() {
	
	double R;
	double eucVol, taxiVol;

	cin >> R;
	
	eucVol = pow(R, 2) * M_PI;
	taxiVol = pow(R, 2) * 2;
	printf("%6f\n%6f", eucVol, taxiVol);

	return 0;
}
